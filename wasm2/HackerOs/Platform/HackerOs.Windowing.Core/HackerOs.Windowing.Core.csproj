<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="..\Packaging.props" />

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <PackageId>HackerOs.Windowing.Core</PackageId>
    <Description>Headless, renderer-independent window manager engine: applies HackerOs.Windowing.Abstractions commands to authoritative window state (focus, z-order, geometry, modality, lifecycle) and emits deterministic events.</Description>
  </PropertyGroup>

  <ItemGroup>
    <!-- Contracts (WindowId, WindowBounds, WindowRuntimeState, commands/events) live in
         HackerOs.Windowing.Abstractions; this project owns only the engine that applies them.
         No browser pipeline, no .razor components, no MudBlazor: this project stays headless. -->
    <ProjectReference Include="..\HackerOs.Windowing.Abstractions\HackerOs.Windowing.Abstractions.csproj" />
  </ItemGroup>

</Project>
